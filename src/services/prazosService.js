// Arquivo preparado para futuras chamadas HTTP/Supabase
export const prazosService = {
  // getPrazos: async () => {},
  // createPrazo: async (data) => {},
  // updatePrazo: async (id, data) => {},
  // deletePrazo: async (id) => {}
};
