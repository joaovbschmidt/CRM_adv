// Arquivo preparado para futuras chamadas HTTP/Supabase
export const processosService = {
  // getProcessos: async () => {},
  // createProcesso: async (data) => {},
  // updateProcesso: async (id, data) => {},
  // deleteProcesso: async (id) => {}
};
